{
  "name": "splitshare-root",
  "version": "1.0.0",
  "description": "Root package - delegates to backend",
  "scripts": {
    "start": "node backend/src/index.js",
    "build": "cd backend && npm install && npx prisma generate && npx prisma db push",
    "install-backend": "cd backend && npm install"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
